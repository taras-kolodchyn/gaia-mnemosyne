# Gaia Mnemosyne

Gaia Mnemosyne — Universal Knowledge Engine (RAG, Ontology, Graph, Embeddings)
Built in Rust + React.

## Features
- Modular ingestion pipelines
- Vector store + Graph store
- Ontology engine
- Ranking engine
- Embeddings via proxy or TensorZero
- AirGap mode
- Full UI + CLI
- Monorepo structure

## Development

```bash
make bootstrap
make up
```
