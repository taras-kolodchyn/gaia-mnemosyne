# mnemo-scheduler

Scheduler primitives for Mnemosyne ingestion jobs.

## Modules
- `queue/` — JobQueue trait
- `runner.rs` — placeholder runner loop

## Usage
Integrated via API job endpoints and ingest job_runner.

## Development
```bash
cargo test -p mnemo-scheduler
```
