name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust 1.91.1
        run: rustup toolchain install 1.91.1 && rustup default 1.91.1
      - name: Placeholder
        run: echo "Gaia Mnemosyne CI pipeline placeholder"
